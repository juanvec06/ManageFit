@model List<NET_MVC.Models.ClienteModel>

@{
    ViewData["Title"] = "Listar Cliente";
    Layout = "~/Views/Entrenador/_LayoutEntrenador.cshtml";
}

<div class="container-grande">
    <div class="text-Asignar" style="text-align: center;
    color: #EE1C25; font-size: 50px; font-style: italic;
    font-weight: 900; line-height: 70px; word-wrap: break-word">
        CLIENTES
    </div>

    <div class="search-bar">
        <label for="identificacion">Identificación:</label>
        <input type="text" id="identificacion" name="identificacion" placeholder="Ingrese la identificación">
        <button type="button" onclick="openModal()">Buscar</button>
    </div>

    <div class="table-container">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Telefono</th>
                    <th>Membresia</th>
                    <th class="small-column">Días restantes de membresía</th>
                </tr>
            </thead>
            <tbody id="tabla-clientes">
                @foreach (var cliente in Model)
                {
                    <tr>
                        <td>@cliente.Identificacion</td>
                        <td>@cliente.Nombre</td>
                        <td>@cliente.Telefono</td>
                        <td>@cliente.refMembresia</td>
                        <td>@cliente.fechaMembresia</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>

<!-- Asegúrate de que jQuery esté cargado -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<script>
    $(document).ready(function () {
        // Cuando se presiona el botón de filtrar
        $('#filtrar').on('click', function () {
            var selectedFilter = $('#filter').val(); // Obtiene el valor del filtro seleccionado

            // Realiza la solicitud Ajax
            $.ajax({
                url: '@Url.Action("Filtrar", "Cliente")', // Acción en el controlador
                type: 'POST',
                data: { filter: selectedFilter }, // Enviamos el filtro seleccionado al servidor
                success: function (response) {

                    // Limpiar el cuerpo de la tabla
                    $('#tabla-clientes').empty();

                    // Recorre los clientes devueltos y los agrega a la tabla
                    $.each(response.clientes, function (index, cliente) {
                        $('#tabla-clientes').append(
                            `<tr>
                                    <td>${cliente.identificacion}</td>
                                    <td>${cliente.nombre}</td>
                                    <td>${cliente.telefono}</td>
                                    <td>${cliente.refMembresia}</td>
                                    <td>${cliente.fechaMembresia}</td>
                                </tr>`
                        );
                    });

                    // Actualiza el total de clientes mostrados
                    $('#total-clientes').text(response.totalClientes);
                },
                error: function () {
                    alert("Error al aplicar el filtro.");
                }
            });
        });
    });
</script>



<style>

    .search-bar {
        display: flex;
        align-items: center;
        margin-bottom: 20px;
        margin-left: 40px;
    }

        .search-bar label {
            padding: 10px 15px;
            border-top-left-radius: 5px;
            border-bottom-left-radius: 5px;
            background-color: #969393;
            border-radius: 10px;
            color: black;
            margin-right: 15px;
        }

        .search-bar input[type="text"] {
            width: 60%;
            border: none;
            outline: none;
            background: none;
            font-size: 1.2rem;
            padding: 10px 10px;
            background: white;
            border-radius: 20px;
            height: 40px;
        }

        .search-bar button {
            background-color: #EE1C25;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 40px;
            margin-left: 10px;
            text-decoration: none;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            width: 190px;
            height: 45px;
            font-family: Inter, sans-serif;
            font-weight: bold;
            font-size: 20px;
            font-style: italic;
            line-height: 70px;
            word-wrap: break-word;
        }


    .filter-total-container {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .total-text {
        margin-left: 30px;
        font-size: 18px;
    }

    .filter-container {
        display: flex;
        align-items: center;
        margin-right: 15px;
        min-width: 300px;
        padding: 10px;
    }

    .container-grande {
        display: flex;
        flex-direction: column;
        width: 100%;
        height: 100%;
    }

    .text-Asignar {
        margin-top: 40px;
        text-align: center;
        color: #EE1C25;
        font-size: 50px;
        font-style: italic;
        font-weight: 900;
        line-height: 70px;
        word-wrap: break-word;
    }

    .filter-container {
        display: flex;
        align-items: center;
        margin-right: 15px;
        border-radius: 20px;
        padding: 10px;
        min-width: 300px;
        margin-left: auto;
    }

    .custom-select {
        margin-top: 10px;
        text-align: center;
        text-align-last: center;
        border-radius: 20px;
        padding: 0;
        font-size: 1.2rem;
        border: 2px solid #dcdcdc;
        appearance: none;
        background-image: url('/img/flechaAbajo.png');
        background-position: right 10px center;
        background-repeat: no-repeat;
        background-size: 15px;
        height: 50px;
        display: flex;
        align-items: center;
        justify-content: center;
    }

        .custom-select option {
            text-align: center;
            padding: 10px;
            font-size: 1.2rem;
        }

    .table-container {
        width: 95%;
        margin: 0 auto;
        max-height: 270px;
        overflow-y: auto;
        margin-top: 10px;
    }

    .table thead th {
        background-color: #b3b3b3;
        color: white;
        text-align: center;
    }

    .table tbody tr td {
        background-color: white;
        text-align: center;
    }

    .table-bordered td, .table-bordered th {
        border: 2px solid #dee2e6;
    }

    .btn {
        background-color: red;
        color: white;
        padding: 10px 10px;
        text-decoration: none;
        justify-content: center;
        align-items: center;
        display: inline-flex;
        font-weight: bold;
        border-radius: 5px;
        transition: background-color 0.3s;
        width: 200px;
        height: 45px;
        padding: 10px;
        font-family: Inter, sans-serif;
        font-weight: bold;
        background: #EE1C25;
        border-radius: 40px;
        font-weight: bold;
        line-height: 70px;
        word-wrap: break-word;
        font-size: 20px;
        font-style: italic;
        margin-left: 130px;
        position: absolute;
        bottom: 20px;
        left: 64%;
    }

        .wrapper .btn:hover {
            background-color: darkred;
        }

    .wrapper a:hover {
        color: #039BE5;
    }

    .table-container::-webkit-scrollbar {
        width: 18px;
    }

    .table-container::-webkit-scrollbar-track {
        background: #ffffff;
        border-radius: 10px;
    }

    .table-container::-webkit-scrollbar-thumb {
        background-color: #4c4c4c;
        border-radius: 10px;
        border: 3px solid #ffffff;
    }

        .table-container::-webkit-scrollbar-thumb:hover {
            background-color: #333333;
        }

    .table-container::-webkit-scrollbar-button:single-button {
        background-color: white;
        display: block;
        height: 18px;
        border-radius: 5px;
        background-size: 8px;
        background-repeat: no-repeat;
        background-position: center;
    }

        .table-container::-webkit-scrollbar-button:single-button:vertical:decrement {
            background-image: url('/img/flechaArriba.png');
        }

        .table-container::-webkit-scrollbar-button:single-button:vertical:increment {
            background-image: url('/img/flechaAbajo.png');
        }

    .table-container::-webkit-scrollbar-button {
        width: 18px;
        height: 18px;
    }

    .table-container::-webkit-scrollbar-corner {
        background: #e0e0e0;
    }

    .small-column {
        width: 200px; /* Puedes ajustar el ancho según lo necesario */
        text-align: center;
    }
</style>