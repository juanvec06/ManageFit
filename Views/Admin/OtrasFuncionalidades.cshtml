@{
    ViewData["Title"] = "Otras Funcionalidades";
    Layout = "~/Views/Admin/_LayoutAdministrador.cshtml";
}

<div class="container-grande">

    <img class="cliente-img" src="/img/user.png" alt="Logo">

    <div id="buscarClienteForm">
        <div class="search-bar">

            <a href="@Url.Action("TransferirSedeCliente", "Cliente")"
               class="btn btn-primary">
                Transferir Cliente
            </a>

            <a href="@Url.Action("BuscarActualizarSalario", "Entrenador")"
               class="btn btn-primary">
                Actualizar Salario
            </a>

        </div>
    </div>

</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>


<style>

    .container-grande {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
    }

    .search-bar {
        width: 400px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
        margin-bottom: 20px;
    }

    .cliente-img {
        width: 100px;
        height: 100px;
        margin-bottom: 20px;
    }


    .container-grande {
        position: relative;
        min-height: 520px;
    }

    .search-bar a {
        background-color: #EE1C25;
        color: white;
        padding: 10px;
        border: none;
        border-radius: 40px;
        margin-left: 10px;
        text-decoration: none;
        display: inline-flex;
        justify-content: center;
        align-items: center;
        width: 190px;
        height: 45px;
        font-family: Inter, sans-serif;
        font-weight: bold;
        font-size: 20px;
        font-style: italic;
        line-height: 70px;
        word-wrap: break-word;
    }

</style>